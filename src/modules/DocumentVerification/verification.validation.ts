// import z from 'zod';

// // Zod validation schema for DocumentVerification
// export const documentVerificationSchema = z.object({
//   body: z.object({
   
//     documentType: z.string().min(1, 'Document type is required'),
//     licenseStatus: z
//       .enum(['pending', 'rejected', 'approved'])
//       .default('pending'),
//   }),
// });
